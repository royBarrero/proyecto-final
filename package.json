{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm install --include=dev && chmod +x ./node_modules/.bin/vite",
    "build": "node ./node_modules/vite/bin/vite.js build",
    "dev": "vite"
  },
  "dependencies": {
    "vite": "^7.0.4",
    "laravel-vite-plugin": "^2.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "axios": "^1.11.0",
    "concurrently": "^9.0.1",
    "tailwindcss": "^4.0.0"
  }
}

